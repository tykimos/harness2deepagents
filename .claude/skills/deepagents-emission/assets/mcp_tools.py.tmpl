"""MCP adapter placeholder generated by harness2deepagents.

The original Harness referenced an .mcp.json configuration. This module is
intentionally conservative — it does not start MCP servers or expose tools
until you review app/.mcp.json and required environment variables.

Detected MCP servers: {{MCP_SERVERS}}
Required env vars:    {{MCP_ENV_VARS}}
"""
from __future__ import annotations


def load_mcp_tools():
    """TODO: Load MCP tools through the appropriate LangChain MCP adapter.

    Steps:
      1. Review ./.mcp.json for server commands and arguments.
      2. Set required environment variables from the list above.
      3. Use langchain-mcp-adapters (or equivalent) to connect.
      4. Return a list of LangChain Tool objects.

    Until implemented this returns an empty list, which keeps the agent
    safely runnable without MCP integration.
    """
    return []
